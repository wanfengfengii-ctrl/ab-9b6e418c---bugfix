[project]
name = "isotope-deconvolver"
version = "1.0.0"
description = "Deterministic deconvolution of overlapping isotope peaks for HRMS review"
requires-python = ">=3.13"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
